/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Tarang2026',
  assetPrefix: '/Tarang2026/',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
